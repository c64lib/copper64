language: asm
sudo: false
before_install:
  - source <(curl -SLs https://raw.githubusercontent.com/c64lib/travis-ci/master/install.sh)
script:
  - cpm common https://github.com/c64lib/common/archive/0.0.4.tar.gz 
  - cpm chipset https://github.com/c64lib/chipset/archive/0.0.2.tar.gz 
  - ka -libdir cpm_modules copper64.asm
  - ka -libdir cpm_modules copper64-test.asm
notifications:
  email:
    on_success: change
    on_failure: change
